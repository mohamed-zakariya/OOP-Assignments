Q8)While the member functions of the same class can use the data,
the variables and data of one class cannot be accessible by another class.

Q9)the variable id is non-static, so it cannot be invoked with the class.